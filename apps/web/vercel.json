{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "pnpm install --filter web... --frozen-lockfile",
  "buildCommand": "pnpm --filter web build",
  "framework": "nextjs"
}
