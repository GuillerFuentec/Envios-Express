{
  "name": "web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "@stripe/stripe-js": "^3.4.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
