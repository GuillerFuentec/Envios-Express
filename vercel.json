{
  "version": 2,
  "builds": [
    {
      "src": "apps/web/next.config.js",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "pnpm install --dir apps/web --filter web... --frozen-lockfile",
  "buildCommand": "pnpm --dir apps/web --filter web build",
  "framework": "nextjs"
}
